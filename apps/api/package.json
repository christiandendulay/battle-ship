{
  "name": "@battleship/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/server.ts --bundle --platform=node --target=node24 --outfile=dist/server.js --packages=bundle",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@battleship/aws-utils": "workspace:*",
    "@types/cors": "^2.8.19",
    "cors": "^2.8.6",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "@battleship/typescript-config": "workspace:*",
    "@types/express": "^4.17.0",
    "@types/node": "^20.19.42",
    "esbuild": "^0.28.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
